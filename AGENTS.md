# Repository Instructions

When I say "promote to production," always rebase the current worktree branch onto `origin/main`, fast-forward merge it into `main`, and push `origin/main`. If the worktree is detached or not on its own branch, stop and report that instead of pushing.
